# v0.2.0-beta.12 · 创作详情与连续浏览

- 稳定画布节点尺寸，旧版手动尺寸不会继续破坏内容排版；保留画布缩放、位置和历史数据。
- 右侧详情打开后，单击其他镜头或素材即可连续查看，不再反复关闭面板。
- 统一素材、生成结果和参考输入的媒体预览，改善横竖图、视频比例及白色边框问题。
- 重整生成记录、模型、提示词、实际输入与参数层级，完整参数仍可查看。
- 精简重复提示词说明，统一参考输入和工作流管理入口。
- 账号弹层支持外部点击关闭及小窗口布局；工作画板明确为镜头总览，保留其排序与粗剪用途。

本轮已通过本地 `pnpm verify`、65 项非 GPU 浏览器测试（无重试），以及最后修正后的 4 项相关回归测试。另完成一次真实 Qwen 图生图参考输入生成，验证素材连接、任务执行、结果回收和参数记录；不将其视为所有模型或硬件的兼容性证明。

安装包构建目标为 macOS、Windows、Linux 的 x64 / arm64；最终产物须经对应包内运行验证。Mac 为临时签名预览包，尚无 Apple 公证；Windows 尚无商业代码签名。ComfyUI、模型和第三方节点不随包安装。原生构建验证不替代用户实机交互验收。

升级前请保存并备份项目，等待生成结束后退出旧应用。连接远程 TakeBoard 项目时，远端服务需单独升级；本机安装新版不会替换远端程序。

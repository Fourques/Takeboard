{
  "name": "@takeboard/test-fixtures",
  "version": "0.2.0-beta.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {}
}
